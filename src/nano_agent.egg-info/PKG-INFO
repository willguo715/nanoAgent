Metadata-Version: 2.4
Name: nano-agent
Version: 0.1.0
Summary: A local CLI agent with tool routing and markdown session memory.
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: openai>=1.40.0
Requires-Dist: tavily-python>=0.5.0
Requires-Dist: pydantic>=2.7.0
Requires-Dist: pyyaml>=6.0.1
Requires-Dist: tenacity>=9.0.0

# nano-agent

一个本地运行的 Python CLI Agent，支持：

- 文本/图片输入
- OpenAI 兼容模型调用
- Tavily 搜索
- Shell 白名单工具（支持用户授权追加）
- 多轮会话与 Markdown 持久化

## 快速开始

1. 安装依赖

```bash
pip install -e .
```

2. 配置

复制 `.env.example` 并设置环境变量，或直接修改 `config.yaml`。

3. 运行

```bash
nano-agent chat --session default
```

或单次执行：

```bash
nano-agent run "帮我总结当前目录结构" --session default
```

图片输入示例：

```bash
nano-agent run "描述这张图重点" --image "D:/path/demo.png" --session default
```
